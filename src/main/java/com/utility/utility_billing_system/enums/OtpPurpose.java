package com.utility.utility_billing_system.enums;

/**
 * Reason an email OTP was issued.
 */
public enum OtpPurpose {
    /** Confirm email for a registered but unverified account (self-signup customers). */
    VERIFY_EMAIL
}
